# Comparison operators

define x = 10;
define y = 5;

sldeploy "x == y: " + (x == y);
sldeploy "x != y: " + (x != y);
sldeploy "x > y: " + (x > y);
sldeploy "x < y: " + (x < y);
sldeploy "x >= y: " + (x >= y);
sldeploy "x <= y: " + (x <= y);
